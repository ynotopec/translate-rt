# Project mind map

```mermaid
mindmap
  root((translate-rt))
    Frontend
      "Flask shell"
        "frontend/app.py"
        "Exposes '/' route"
        "Serves static assets"
      "Static UI"
        "frontend/static/html/index.html"
        "DSFR layout + controls"
      "Recorder & UI logic"
        "frontend/static/js/code.js"
        "Modules"
          "Utilities & DOM bindings"
          "Config: Net, Limits, VADParams"
          "Centralised State store"
          "Recorder lifecycle"
          "Custom VAD loop"
          "Network uploads & responses"
          "Renderer for transcript/translation"
          "TTS queue + playback"
        "Helpers"
          "Error banners"
          "JSON export"
    "FastAPI backend"
      "api-translate-rt/app.py"
      "Services"
        "Environment-driven Cfg"
        "requests Session pooling"
        "Whisper transcription proxy"
        "Optional diarisation"
        "LRU-cached translations"
      "Endpoints"
        "/upload"
          "Validate chunk size"
          "Invoke diarisation + Whisper"
          "Filter noise/empty text"
          "Detect language"
          "Assemble translations"
        "/translate-text"
          "Pydantic request/response"
          "Reuse translate_text()"
        "/tts-proxy"
          "Relay JSON payload to TTS"
      "Entrypoint"
        "uvicorn runner"
    "Realtime WebSocket API"
      "api-realtime-ai-futur/app.py"
      "Config helpers"
        "Env coercion utilities"
        "Payload preview logging"
      "Conversation state"
        "WSConv history store"
        "Session configuration"
        "OpenAI chat prompts"
      "Audio processing"
        "PCM resampling buffers"
        "WebRTC VAD turn detection"
        "Whisper transcription bridge"
      "Response pipeline"
        "Background response threads"
        "Chat completions"
        "TTS synthesis & streaming"
      "WebSocket handling"
        "Auth token checks"
        "Session/update events"
        "Audio buffer commands"
        "Error reporting"
    Docs
      "README.md overview"
      "docs/streaming-sequence.md"
      "frontend/app_diagram.md"
```

This Mermaid mind map highlights the major subsystems, files and responsibilities in the real-time translation project.
